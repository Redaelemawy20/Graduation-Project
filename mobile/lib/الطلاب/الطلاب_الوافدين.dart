import 'package:flutter/material.dart';

class wafden extends StatelessWidget {
  const wafden({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: double.infinity,
      decoration: BoxDecoration(
        color: Colors.white,
      ),
      child: Text('إدراة الوافدين'),
    );
  }
}
